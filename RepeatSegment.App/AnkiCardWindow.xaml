<Window x:Class="RepeatSegment.App.AnkiCardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Anki Card" Width="700" Height="720"
        MinWidth="600" MinHeight="600"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackgroundBrush}"
        FontFamily="Segoe UI" FontSize="12pt">
    <Window.Resources>
        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#F0F2F5"/>
        <SolidColorBrush x:Key="TextBrush" Color="#222222"/>
        <SolidColorBrush x:Key="TextDimBrush" Color="#666666"/>
        <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SeparatorBrush" Color="#CCCCCC"/>
        <SolidColorBrush x:Key="ButtonBgBrush" Color="#E0E0E0"/>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>   <!-- 0: Deck -->
            <RowDefinition Height="Auto"/>   <!-- 1: EN -->
            <RowDefinition Height="Auto"/>   <!-- 2: Transcription -->
            <RowDefinition Height="Auto"/>   <!-- 3: RU -->
            <RowDefinition Height="Auto"/>   <!-- 4: Context -->
            <RowDefinition Height="Auto"/>   <!-- 5: Sentence -->
            <RowDefinition Height="Auto"/>   <!-- 6: TTS/Record selection -->
            <RowDefinition Height="*" MinHeight="160"/> <!-- 7: Image search -->
            <RowDefinition Height="Auto"/>   <!-- 8: Status -->
            <RowDefinition Height="Auto"/>   <!-- 9: Buttons -->
        </Grid.RowDefinitions>

        <!-- Deck selector -->
        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Deck:" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}" Margin="0,0,8,0"/>
            <ComboBox Grid.Column="1" x:Name="CmbDeck" MinWidth="150" VerticalAlignment="Center"/>
            <Button Grid.Column="2" x:Name="BtnNewDeck" Content="+ New" Padding="6,2" Margin="4,0,0,0" Click="BtnNewDeck_Click" Background="{DynamicResource ButtonBgBrush}"/>
            <Button Grid.Column="3" x:Name="BtnOpenDeck" Content="Open" Padding="6,2" Margin="4,0,0,0" Click="BtnOpenDeck_Click" Background="{DynamicResource ButtonBgBrush}" ToolTip="Open selected .apkg file"/>
        </Grid>

        <!-- EN -->
        <Grid Grid.Row="1" Margin="0,0,0,6">
            <Grid.ColumnDefinitions><ColumnDefinition Width="80"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="EN:" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}" FontWeight="Bold"/>
            <TextBox Grid.Column="1" x:Name="TxtEn" Padding="4"/>
        </Grid>

        <!-- Transcription -->
        <Grid Grid.Row="2" Margin="0,0,0,6">
            <Grid.ColumnDefinitions><ColumnDefinition Width="80"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Transcription:" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>
            <TextBox Grid.Column="1" x:Name="TxtTranscription" Padding="4"/>
        </Grid>

        <!-- RU -->
        <Grid Grid.Row="3" Margin="0,0,0,6">
            <Grid.ColumnDefinitions><ColumnDefinition Width="80"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="RU:" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}" FontWeight="Bold"/>
            <TextBox Grid.Column="1" x:Name="TxtRu" Padding="4"/>
        </Grid>

        <!-- Context -->
        <Grid Grid.Row="4" Margin="0,0,0,6">
            <Grid.ColumnDefinitions><ColumnDefinition Width="80"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Context:" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>
            <TextBox Grid.Column="1" x:Name="TxtContext" Padding="4" TextWrapping="Wrap" MinHeight="40" MaxHeight="60" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
        </Grid>

        <!-- Audio: sentence from book -->
        <Grid Grid.Row="5" Margin="0,0,0,4">
            <Grid.ColumnDefinitions><ColumnDefinition Width="80"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Sentence:" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}" FontSize="11"/>
            <TextBox Grid.Column="1" x:Name="TxtSentenceAudio" Padding="4,2" IsReadOnly="True" Background="#F5F5F5" FontSize="11" Height="24" VerticalAlignment="Center"/>
            <Button Grid.Column="2" x:Name="BtnPreviewSentence" Content="▶ Play" Padding="6,2" Margin="4,0,0,0" Click="BtnPreviewSentence_Click" Background="{DynamicResource ButtonBgBrush}" MinWidth="60" FontSize="11"/>
        </Grid>
        <!-- TTS provider selection -->
        <Grid Grid.Row="6" Margin="0,2,0,2">
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <!-- Google (default) -->
            <Grid Grid.Row="0" Margin="0,0,0,2">
                <Grid.ColumnDefinitions><ColumnDefinition Width="24"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <RadioButton Grid.Column="0" x:Name="RbGoogle" VerticalAlignment="Center" GroupName="TtsProvider" Checked="RbGoogle_Checked" IsChecked="True"/>
                <TextBlock Grid.Column="1" Text="Google:" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}" FontSize="11" Margin="4,0,8,0"/>
                <TextBox Grid.Column="2" x:Name="TxtGoogleAudio" Padding="4,0" IsReadOnly="True" Background="#F5F5F5" FontSize="11" Height="22" VerticalAlignment="Center"/>
                <Button Grid.Column="3" x:Name="BtnPreviewGoogle" Content="▶ Preview" Padding="4,0" Margin="4,0,0,0" Click="BtnPreviewGoogle_Click" Background="{DynamicResource ButtonBgBrush}" MinWidth="60" FontSize="11" Height="22"/>
            </Grid>
            <!-- Deepgram -->
            <Grid Grid.Row="1" Margin="0,0,0,2">
                <Grid.ColumnDefinitions><ColumnDefinition Width="24"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <RadioButton Grid.Column="0" x:Name="RbDeepgram" VerticalAlignment="Center" GroupName="TtsProvider" Checked="RbDeepgram_Checked"/>
                <TextBlock Grid.Column="1" Text="Deepgram:" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}" FontSize="11" Margin="4,0,8,0"/>
                <TextBox Grid.Column="2" x:Name="TxtDeepgramAudio" Padding="4,0" IsReadOnly="True" Background="#F5F5F5" FontSize="11" Height="22" VerticalAlignment="Center"/>
                <Button Grid.Column="3" x:Name="BtnPreviewDeepgram" Content="▶ Preview" Padding="4,0" Margin="4,0,0,0" Click="BtnPreviewDeepgram_Click" Background="{DynamicResource ButtonBgBrush}" MinWidth="60" FontSize="11" Height="22"/>
            </Grid>
            <!-- Record (radio button + Rec + Play) -->
            <Grid Grid.Row="2" Margin="0,0,0,2">
                <Grid.ColumnDefinitions><ColumnDefinition Width="24"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <RadioButton Grid.Column="0" x:Name="RbRecord" VerticalAlignment="Center" GroupName="TtsProvider" Checked="RbRecord_Checked"/>
                <TextBlock Grid.Column="1" Text="Record:" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}" FontSize="11" Margin="4,0,8,0"/>
                <TextBox Grid.Column="2" x:Name="TxtRecAudio" Padding="4,0" IsReadOnly="True" Background="#F5F5F5" FontSize="11" Height="22" VerticalAlignment="Center" Text="(click 🎤 Rec to record microphone)"/>
                <Button Grid.Column="3" x:Name="BtnRecordAudio" Content="🎤 Rec" Padding="4,0" Margin="4,0,0,0" Click="BtnRecordAudio_Click" Background="{DynamicResource ButtonBgBrush}" MinWidth="55" FontSize="11" Height="22" ToolTip="Record from microphone"/>
                <Button Grid.Column="4" x:Name="BtnPlayRecord" Content="▶ Play" Padding="4,0" Margin="4,0,0,0" Click="BtnPlayRecord_Click" Background="{DynamicResource ButtonBgBrush}" MinWidth="55" FontSize="11" Height="22" ToolTip="Play recorded audio"/>
            </Grid>
        </Grid>

        <!-- Image search -->
        <Grid Grid.Row="7" Margin="0,0,0,6">
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
            <Grid Grid.Row="0" Margin="0,0,0,4">
                <Grid.ColumnDefinitions><ColumnDefinition Width="80"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Picture:" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>
                <TextBox Grid.Column="1" x:Name="TxtSearchQuery" Padding="4"/>
                <Button Grid.Column="2" x:Name="BtnSearchImages" Content="Search" Padding="8,2" Margin="8,0,4,0" Click="BtnSearchImages_Click" Background="{DynamicResource ButtonBgBrush}"/>
                <Button Grid.Column="3" x:Name="BtnDownloadImage" Content="✓ Use" Padding="8,2" Click="BtnDownloadImage_Click" Background="{DynamicResource ButtonBgBrush}"/>
            </Grid>
            <wv2:WebView2 Grid.Row="1" x:Name="ImageBrowser"/>
            <Border Grid.Row="1" x:Name="PicturePreview" Visibility="Collapsed" BorderBrush="{DynamicResource SeparatorBrush}" BorderThickness="1" CornerRadius="4">
                <Grid>
                    <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                    <Image Grid.Row="0" x:Name="ImgPreview" Stretch="Uniform"/>
                    <Button Grid.Row="1" Content="🔍 Search again" HorizontalAlignment="Center" Margin="0,4,0,0"
                            Padding="10,3" Click="BtnSearchAgain_Click" Background="{DynamicResource ButtonBgBrush}"/>
                </Grid>
            </Border>
        </Grid>

        <!-- Status -->
        <TextBlock Grid.Row="8" x:Name="TxtStatus" Foreground="{DynamicResource TextDimBrush}" Margin="0,0,0,6" FontSize="11"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="9" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="BtnCancel" Content="Cancel" Width="80" Padding="8,4" Margin="0,0,10,0" Click="BtnCancel_Click" Background="{DynamicResource ButtonBgBrush}"/>
            <Button x:Name="BtnCreate" Content="Create Cards" Width="120" Padding="8,4" Click="BtnCreate_Click" Background="#FF56B4E9" Foreground="White" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>
